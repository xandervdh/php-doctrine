{% extends 'base.html.twig' %}

{% block title %}Hello RestController!{% endblock %}

{% block body %}
    {{ json }}
<h3>{{ message }}</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit"  value="submit"/>
    {{ form_end(form) }}

{% endblock %}
